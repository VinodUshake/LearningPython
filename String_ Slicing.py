#Slicing = Create a substring by  by extracting elements from another string
# indexing [] or Slice()
# Slicing funtion there are 3 arguments, depending on where and how to slice string
# Staring index, stoping index and step [Start:stop:step]
# let's being with Starting Index
# lets say we have string

name = "vinod Ushaker"
#create a substring based on slice portion  " name")

first_name = name[0]
# we need starting and stoping index.  where do want to being our slice, it's always starts with 0
print(first_name)

