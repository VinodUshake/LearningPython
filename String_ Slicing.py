#Slicing = Create a substring by  by extracting elements from another string
# indexing [] or Slice()
# Slicing function there are 3 arguments, depending on where and how to slice string
# Staring  3 arguments,  index, stopping index and step [start:stop:step]
# let being with Starting Index
# lets say we have string

name = "vinodUshaker"
#create a substring based on slice portion  "name")

first_name = name[2:7]
# we need starting and stopping index.  where do want to begin our slice, it's always starts with 0
print(first_name)

